"Assistant is thinking" three-dot indicator, styled as an assistant bubble.

```jsx
{loading && <TypingDots />}
```

Render in the message list while awaiting the AI response, then replace with a `ChatBubble`.
